import { LoginForm } from '@/components/LoginForm';

const RegisterPage = () => {
  return (
    <div className="flex">
      <LoginForm mode="register" />
    </div>
  );
};

export default RegisterPage;
